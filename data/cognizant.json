[
  { "id": 1, "section": "online-test", "title": "What does the Cognizant GenC assessment cover?", "difficulty": "Easy", "answer": "The GenC assessment (AMCAT-based) typically covers Quantitative Aptitude, Logical Reasoning, English Comprehension, Automata/coding, and sometimes a Personality/WriteX section for communication scoring." },
  { "id": 2, "section": "online-test", "title": "What is the difference between GenC, GenC Next, and GenC Elevate?", "difficulty": "Medium", "answer": "GenC is the standard fresher engineering trainee track, GenC Next targets candidates with stronger coding skills for slightly more technical roles, and GenC Elevate targets candidates from non-CS backgrounds with an extended technical training program before project allocation." },
  { "id": 3, "section": "technical", "title": "What is the difference between primary key and foreign key?", "difficulty": "Easy", "answer": "A primary key uniquely identifies each record in its own table and cannot be null, while a foreign key is a column in one table that references the primary key of another table, enforcing referential integrity between them.", "example": "CREATE TABLE Orders (\n  OrderID INT PRIMARY KEY,\n  CustomerID INT,\n  FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID)\n);" },
  { "id": 4, "section": "technical", "title": "What is method overloading vs method overriding?", "difficulty": "Medium", "answer": "Overloading is defining multiple methods with the same name but different parameter lists within the same class (compile-time polymorphism). Overriding is redefining a parent class's method in a child class with the same signature (runtime polymorphism).", "example": "// Overloading\nint add(int a,int b){return a+b;}\ndouble add(double a,double b){return a+b;}\n// Overriding\nclass Animal{ void sound(){} }\nclass Dog extends Animal{ void sound(){ System.out.println(\"Bark\"); } }" },
  { "id": 5, "section": "technical", "title": "What is the difference between array and ArrayList in Java?", "difficulty": "Medium", "answer": "An array has a fixed size defined at creation and can hold primitives or objects, while an ArrayList is a resizable collection from java.util that only holds objects (autoboxing primitives) and offers built-in methods like add(), remove(), and contains()." },
  { "id": 6, "section": "technical", "title": "Explain the OSI model briefly.", "difficulty": "Medium", "answer": "The OSI model has seven layers describing how data travels across a network: Physical, Data Link, Network, Transport, Session, Presentation, and Application. Each layer has a specific responsibility, from raw bit transmission (Physical) to user-facing services (Application).", "tips": "Use the mnemonic 'Please Do Not Throw Sausage Pizza Away' to recall the order." },
  { "id": 7, "section": "coding", "title": "Write a program to find the sum of digits of a number.", "difficulty": "Easy", "answer": "Repeatedly extract the last digit using modulo 10 and add it to a running sum, then remove the last digit using integer division by 10.", "example": "int sumOfDigits(int n){\n  int sum=0;\n  while(n>0){\n    sum += n%10;\n    n /= 10;\n  }\n  return sum;\n}" },
  { "id": 8, "section": "coding", "title": "Write a program to print the Fibonacci series up to n terms.", "difficulty": "Easy", "answer": "Start with the first two terms as 0 and 1, then generate each subsequent term as the sum of the previous two.", "example": "def fibonacci(n):\n    a, b = 0, 1\n    for _ in range(n):\n        print(a, end=' ')\n        a, b = b, a + b" },
  { "id": 9, "section": "coding", "title": "Write a function to check if a given number is an Armstrong number.", "difficulty": "Medium", "answer": "An Armstrong number equals the sum of its own digits each raised to the power of the number of digits. Compute digit count first, then sum each digit raised to that power and compare with the original number.", "example": "boolean isArmstrong(int n){\n  int orig=n, digits=String.valueOf(n).length(), sum=0;\n  while(n>0){\n    int d=n%10;\n    sum += Math.pow(d, digits);\n    n /= 10;\n  }\n  return sum==orig;\n}" },
  { "id": 10, "section": "hr", "title": "Tell us about a time you worked in a team.", "difficulty": "Medium", "answer": "Use the STAR method — Situation, Task, Action, Result — to structure a concise, specific story about a college project or hackathon, emphasizing your individual contribution and the outcome.", "example": "\"During our final-year project, our team split a hospital-management system into modules. I owned the appointment-booking module, coordinated API contracts with two teammates, and we delivered on time with zero integration bugs.\"" },
  { "id": 11, "section": "hr", "title": "How do you handle pressure or tight deadlines?", "difficulty": "Easy", "answer": "Give a concrete example showing a practical technique — prioritizing tasks, breaking work into smaller chunks, or communicating early about blockers — rather than a vague claim like 'I stay calm.'" },
  { "id": 12, "section": "hr", "title": "What are your salary expectations?", "difficulty": "Medium", "answer": "For freshers, it's usually safest to say you are open to the company's standard package for the role and are more focused on learning and growth opportunities at this stage, unless you have a specific, researched figure in mind." },
  { "id": 13, "section": "technical", "title": "What is the difference between a shallow copy and a deep copy of an object?", "difficulty": "Medium", "answer": "A shallow copy duplicates an object but keeps references to the same nested objects it contains, so changes to nested data affect both copies. A deep copy recursively duplicates every nested object as well, producing a fully independent clone.", "example": "// Java example using clone() (shallow by default)\nclass Box implements Cloneable {\n  int[] items;\n  public Object clone() throws CloneNotSupportedException { return super.clone(); }\n}" },
  { "id": 14, "section": "technical", "title": "What is a race condition, and how can it be prevented?", "difficulty": "Medium", "answer": "A race condition occurs when two or more threads access shared data concurrently and the final result depends on the unpredictable timing of their execution. It's prevented using synchronization mechanisms like locks, synchronized blocks, or atomic variables to ensure only one thread modifies shared state at a time." },
  { "id": 15, "section": "technical", "title": "What is the difference between IPv4 and IPv6?", "difficulty": "Easy", "answer": "IPv4 uses 32-bit addresses (about 4.3 billion possible addresses) written in dotted-decimal form, while IPv6 uses 128-bit addresses written in hexadecimal, offering a vastly larger address space to accommodate the growing number of internet-connected devices." },
  { "id": 16, "section": "technical", "title": "What is the difference between a session and a cookie in web applications?", "difficulty": "Easy", "answer": "A cookie is a small piece of data stored on the client's browser and sent with every request to the same domain. A session stores user data on the server side, with only a session ID (often stored in a cookie) shared with the client, making sessions generally more secure for sensitive data." },
  { "id": 17, "section": "technical", "title": "What are the SOLID principles of object-oriented design?", "difficulty": "Hard", "answer": "SOLID stands for Single Responsibility (a class should have one reason to change), Open/Closed (open for extension, closed for modification), Liskov Substitution (subtypes must be substitutable for their base types), Interface Segregation (prefer many small interfaces over one large one), and Dependency Inversion (depend on abstractions, not concrete implementations)." },
  { "id": 18, "section": "technical", "title": "What is the difference between authentication and authorization?", "difficulty": "Easy", "answer": "Authentication verifies who a user is (e.g., logging in with a username and password), while authorization determines what an already-authenticated user is allowed to do or access (e.g., admin vs regular-user permissions)." },
  { "id": 19, "section": "coding", "title": "Write a program to find the longest common prefix among an array of strings.", "difficulty": "Medium", "answer": "Take the first string as an initial guess for the prefix, then progressively shorten it until it matches the start of every other string in the array.", "example": "String longestCommonPrefix(String[] strs){\n  if(strs.length==0) return \"\";\n  String prefix = strs[0];\n  for(int i=1;i<strs.length;i++){\n    while(!strs[i].startsWith(prefix)){\n      prefix = prefix.substring(0, prefix.length()-1);\n    }\n  }\n  return prefix;\n}" },
  { "id": 20, "section": "coding", "title": "Write a program to check for balanced parentheses in an expression.", "difficulty": "Medium", "answer": "Use a stack: push every opening bracket, and on each closing bracket, pop and check it matches the corresponding opening bracket. The expression is balanced only if the stack is empty at the end.", "example": "boolean isBalanced(String s){\n  Deque<Character> stack = new ArrayDeque<>();\n  for(char c: s.toCharArray()){\n    if(c=='('||c=='['||c=='{') stack.push(c);\n    else {\n      if(stack.isEmpty()) return false;\n      char top = stack.pop();\n      if((c==')' && top!='(') || (c==']' && top!='[') || (c=='}' && top!='{')) return false;\n    }\n  }\n  return stack.isEmpty();\n}" },
  { "id": 21, "section": "coding", "title": "Write a program to merge two sorted arrays into one sorted array.", "difficulty": "Medium", "answer": "Use two pointers, one for each array, repeatedly picking the smaller current element and advancing that pointer until both arrays are exhausted.", "example": "int[] merge(int[] a, int[] b){\n  int[] result = new int[a.length+b.length];\n  int i=0,j=0,k=0;\n  while(i<a.length && j<b.length)\n    result[k++] = a[i]<=b[j] ? a[i++] : b[j++];\n  while(i<a.length) result[k++]=a[i++];\n  while(j<b.length) result[k++]=b[j++];\n  return result;\n}" },
  { "id": 22, "section": "coding", "title": "Write a program to find the first non-repeating character in a string.", "difficulty": "Medium", "answer": "Count the frequency of each character using a HashMap, then iterate through the string again in order and return the first character whose count is exactly one.", "example": "Character firstNonRepeating(String s){\n  Map<Character,Integer> freq = new LinkedHashMap<>();\n  for(char c: s.toCharArray()) freq.put(c, freq.getOrDefault(c,0)+1);\n  for(char c: s.toCharArray()) if(freq.get(c)==1) return c;\n  return null;\n}" },
  { "id": 23, "section": "coding", "title": "Write a program to implement insertion sort.", "difficulty": "Easy", "answer": "Build the sorted array one element at a time, taking each new element and inserting it into its correct position among the already-sorted elements before it.", "example": "void insertionSort(int[] arr){\n  for(int i=1;i<arr.length;i++){\n    int key=arr[i], j=i-1;\n    while(j>=0 && arr[j]>key){\n      arr[j+1]=arr[j]; j--;\n    }\n    arr[j+1]=key;\n  }\n}" },
  { "id": 24, "section": "coding", "title": "Write a SQL query to find the total salary paid per department.", "difficulty": "Easy", "answer": "Group employees by department and sum their salary column within each group.", "example": "SELECT dept_id, SUM(salary) AS total_salary\nFROM Employee\nGROUP BY dept_id;" }
]
